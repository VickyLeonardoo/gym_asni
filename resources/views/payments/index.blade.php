<x-app-layout>
    <x-slot name="header"><h2 class="text-xl font-semibold text-gray-800">Payment Proofs</h2></x-slot>
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <form method="GET" class="mb-4 flex gap-2"><select name="status" class="rounded-md border-gray-300 text-sm"><option value="">All status</option>@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(request('status') === $status->value)>{{ $status->label() }}</option>@endforeach</select><button class="rounded-md border px-3 text-sm"><i class="bi bi-filter"></i></button></form>
        <div class="overflow-hidden rounded-md border bg-white"><table class="min-w-full divide-y divide-gray-200 text-sm"><thead class="bg-gray-50 text-left"><tr><th class="px-4 py-3">Member</th><th>Plan</th><th>Amount</th><th>Status</th><th>Proof</th><th></th></tr></thead><tbody class="divide-y divide-gray-100">@forelse($payments as $payment)<tr><td class="px-4 py-3 font-medium">{{ $payment->membership->member->name }}</td><td>{{ $payment->membership->plan->name }}</td><td>Rp {{ number_format($payment->amount, 0, ',', '.') }}</td><td>{{ $payment->status->label() }}</td><td>@if($payment->proof_path)<a href="{{ Storage::url($payment->proof_path) }}" target="_blank">Open</a>@else - @endif</td><td class="px-4 text-right"><form method="POST" action="{{ route('payments.verify', $payment) }}" class="inline">@csrf @method('PATCH')<button class="text-emerald-700">Verify</button></form><form method="POST" action="{{ route('payments.verify', $payment) }}" class="inline ms-3">@csrf @method('PATCH')<input type="hidden" name="reject" value="1"><button class="text-red-700">Reject</button></form></td></tr>@empty @include('shared.table-empty', ['cols' => 6]) @endforelse</tbody></table></div>
        <div class="mt-4">{{ $payments->links() }}</div>
    </div>
</x-app-layout>
