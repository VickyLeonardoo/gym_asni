<x-app-layout>
    <x-slot name="header"><div class="flex items-center justify-between"><h2 class="text-xl font-semibold text-gray-800">Membership Detail</h2><a href="{{ route('payments.edit', $membership) }}" class="rounded-md bg-gray-900 px-4 py-2 text-sm font-semibold text-white"><i class="bi bi-upload"></i> Payment Proof</a></div></x-slot>
    <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 space-y-6">
        <div class="rounded-md border bg-white p-6 grid gap-4 sm:grid-cols-3 text-sm"><div><span class="text-gray-500">Member</span><p class="font-medium">{{ $membership->member->name }}</p></div><div><span class="text-gray-500">Plan</span><p class="font-medium">{{ $membership->plan->name }}</p></div><div><span class="text-gray-500">Status</span><p class="font-medium">{{ $membership->status->label() }}</p></div><div><span class="text-gray-500">Starts</span><p>{{ $membership->starts_at->format('d M Y') }}</p></div><div><span class="text-gray-500">Expires</span><p>{{ $membership->expires_at->format('d M Y') }}</p></div><div><span class="text-gray-500">Price</span><p>Rp {{ number_format($membership->price, 0, ',', '.') }}</p></div></div>
        <div class="rounded-md border bg-white overflow-hidden"><div class="px-4 py-3 font-semibold">Payments</div><table class="min-w-full divide-y divide-gray-200 text-sm"><thead class="bg-gray-50 text-left"><tr><th class="px-4 py-3">Amount</th><th>Paid At</th><th>Method</th><th>Status</th><th>Proof</th></tr></thead><tbody class="divide-y divide-gray-100">@foreach($membership->payments as $payment)<tr><td class="px-4 py-3">Rp {{ number_format($payment->amount, 0, ',', '.') }}</td><td>{{ $payment->paid_at?->format('d M Y') ?: '-' }}</td><td>{{ $payment->method }}</td><td>{{ $payment->status->label() }}</td><td>@if($payment->proof_path)<a target="_blank" href="{{ Storage::url($payment->proof_path) }}" class="text-gray-900">Open</a>@else - @endif</td></tr>@endforeach</tbody></table></div>
    </div>
</x-app-layout>
