<x-app-layout>
    <x-slot name="header"><h2 class="text-xl font-semibold text-gray-800">Assets</h2></x-slot>
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="mb-4 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><form method="GET" class="flex flex-wrap gap-2"><input name="search" value="{{ request('search') }}" placeholder="Search assets" class="rounded-md border-gray-300 text-sm"><select name="status" class="rounded-md border-gray-300 text-sm"><option value="">All status</option>@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(request('status') === $status->value)>{{ $status->label() }}</option>@endforeach</select><select name="condition" class="rounded-md border-gray-300 text-sm"><option value="">All condition</option>@foreach($conditions as $condition)<option value="{{ $condition->value }}" @selected(request('condition') === $condition->value)>{{ $condition->label() }}</option>@endforeach</select><button class="rounded-md border px-3 text-sm"><i class="bi bi-search"></i></button></form><a href="{{ route('assets.create') }}" class="inline-flex items-center gap-2 rounded-md bg-gray-900 px-4 py-2 text-sm font-semibold text-white"><i class="bi bi-plus-lg"></i> Asset</a></div>
        <div class="overflow-hidden rounded-md border bg-white"><table class="min-w-full divide-y divide-gray-200 text-sm"><thead class="bg-gray-50 text-left"><tr><th class="px-4 py-3">Code</th><th>Name</th><th>Category</th><th>Status</th><th>Condition</th><th>Last Maintenance</th><th>Next Due</th><th></th></tr></thead><tbody class="divide-y divide-gray-100">@forelse($assets as $asset)@php($nextDue = $asset->nextMaintenanceDueDate())<tr><td class="px-4 py-3">{{ $asset->asset_code }}</td><td class="font-medium"><a href="{{ route('assets.show', $asset) }}">{{ $asset->name }}</a><p class="text-xs font-normal text-gray-500">Every {{ $asset->maintenance_interval_months }} months</p></td><td>{{ $asset->category }}</td><td>{{ $asset->status->label() }}</td><td>{{ $asset->condition->label() }}</td><td>{{ $asset->lastMaintenanceDate()?->format('d M Y') ?? '-' }}</td><td>@if($nextDue)<span class="{{ $nextDue->isPast() ? 'text-red-700 font-semibold' : ($nextDue->diffInDays(now()) <= 30 ? 'text-amber-700 font-semibold' : '') }}">{{ $nextDue->format('d M Y') }}</span>@else - @endif</td><td class="px-4 text-right"><a href="{{ route('assets.edit', $asset) }}"><i class="bi bi-pencil-square"></i></a></td></tr>@empty @include('shared.table-empty', ['cols' => 8]) @endforelse</tbody></table></div>
        <div class="mt-4">{{ $assets->links() }}</div>
    </div>
</x-app-layout>
